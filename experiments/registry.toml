# @dependency-start
# contract registry
# responsibility Registers experiment metadata for template experiment workflows.
# upstream design ../documents/design/experiment-workflow.md experiment registry contract
# downstream design ./README.md documents parent-owned execution and result paths
# @dependency-end
schema_version = 1

[defaults]
report_root = "experiments/report"
integration_branch = "main"
required_eval_artifacts = ["summary.json", "cases.jsonl", "config.json"]
optional_eval_artifacts = ["logs/*.log", "logs/*.txt", "logs/*.jsonl"]

# @dependency-start
# responsibility Declares repo Python dependencies installed after workspace mount.
# upstream design ../vendor/agent-canon/CONTAINER_OPERATIONS.md keeps repo requirements out of Docker image build.
# downstream implementation install_python_dependencies.sh installs these dependencies.
# downstream implementation ../.github/workflows/ci.yml installs these dependencies for CI.
# @dependency-end

experiment-runner
jaxtyping
typeguard
equinox
optax
scipy
scipy-stubs
flatbuffers
iree-base-compiler
iree-base-runtime

# Testing
pytest>=8

# Development & Analysis
pyright
pydeps
pipdeptree
deptry
snakeviz
psutil
pydocstyle
ruff

# Documentation & Formatting
mdformat
mdformat-myst
pyyaml>=6
tomli>=2.0.1

# Security & Audit
jsonschema>=4.0.0
cryptography>=41.0.0

# Data validation
pydantic>=2.0.0

# Notebook visualization
matplotlib
jupyterlab
notebook
ipykernel

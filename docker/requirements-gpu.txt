# @dependency-start
# contract environment
# responsibility Owns the GPU-only Python lock layered on the CPU base lock.
# upstream environment ./requirements.txt provides the CPU dependency base.
# upstream implementation ./Dockerfile installs this lock into the gpu-dev image stage.
# downstream environment ./Dockerfile materializes the GPU development profile.
# @dependency-end

# GPU-only Python additions. docker/Dockerfile first copies the CPU venv, then
# installs this hash-locked delta in target gpu-dev.
jax-cuda12-plugin==0.10.2 \
    --hash=sha256:76bbfece2f6effb4e8908eb8a5fbf1abbc1125de667683ec2ffba51c63dc57af
jax-cuda12-pjrt==0.10.2 \
    --hash=sha256:806d1fd29038b6acf5a2b289dd62192abea977ee26aef60ea295b1d28a23acf8

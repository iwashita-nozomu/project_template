# @dependency-start
# contract environment
# responsibility Defines the host Docker container pack configuration.
# upstream environment ../README.md docker runtime guidance
# upstream environment ../Dockerfile default runtime image
# upstream design ../vendor/agent-canon/documents/design/devcontainer/parent-devcontainer-policy.md runtime shell ownership
# @dependency-end
[pack]
name = "default-host-docker"
dockerfile = "docker/Dockerfile"
context = "."
image_tag = "project-template:default-runtime-pack-host-docker"

[smoke]
shell = "/bin/bash"
commands = [
  "python3 --version",
  "docker version",
]

[runtime]
shell = "/bin/zsh"
workdir = "/workspace"
workspace_mount = "/workspace"
mounts = [
  "/var/run/docker.sock:/var/run/docker.sock",
]

# 数値安定性

この章は、数値安定性に関する最低限の基準をまとめます。

## 要約

- ゼロ除算を避けます。
- 悪条件問題ではログを残します。

## 規約

- ゼロ除算を防ぐため `AVOID_ZERO_DIV` を使い、`jnp.where` で安全化します。
- `EPS` / `WEAK_EPS` の使い分けは、停止判定・正則化など目的をコメントで明記します。
- 悪条件問題では、**相対誤差と残差の両方をログ出力**して追跡します。

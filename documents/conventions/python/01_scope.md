# 対象

## 要約

- 対象は `python/jax_util/` 配下の Python 実装です。
- 主対象は安定サブモジュールです。

## 規約

- この文書は、`python/jax_util/` 配下の実装を対象にします。
- 安定サブモジュールは `base` / `solvers` / `optimizers` / `hlo` とします。
- `neuralnetwork` は実験段階のため、必要最小限の補足を除き安定規約の基準からは外します。

# @dependency-start
# contract configuration
# responsibility Aggregates C++ validation cases for the root library build.
# upstream design ../../documents/design/cpp-build-layout.md test ownership
# downstream implementation version/CMakeLists.txt version validation project
# @dependency-end

add_subdirectory(version)

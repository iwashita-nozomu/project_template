# C++ test

`version_test.cpp` is the concrete CTest smoke test for the root
`project::core` library. Add product C++ tests to this directory and register
them in `CMakeLists.txt`; do not create another language-local test root.

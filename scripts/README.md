# Repository bootstrap scripts

`init_from_template.sh` performs the offline identity conversion.
`start_repository.sh` adds a preview and read-only post-commit validation
wrapper. Both operate only on the project checkout.
